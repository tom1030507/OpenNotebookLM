# Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.32
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
sqlite-vec==0.1.9

# Document Processing
PyMuPDF==1.23.8
pdfminer.six==20221105
beautifulsoup4==4.12.2
lxml==4.9.3
readability-lxml==0.8.1
youtube-transcript-api==0.6.1

# ML/NLP
numpy==1.24.3
sentence-transformers==2.2.2
# sentence-transformers 2.2.2 still imports cached_download, which was removed
# from newer huggingface-hub releases despite its open-ended lower-bound pin.
huggingface-hub==0.25.2
transformers==4.48.3
faiss-cpu==1.7.4
llama-cpp-python==0.2.20
openai==1.3.7
# 0.8.0 predates the Messages API entirely (no client.messages), so it cannot
# talk to any current Claude model.
anthropic==0.122.0
tiktoken==0.5.1

# Utils
httpx==0.25.2
aiofiles==23.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Password hashing calls bcrypt directly: passlib 1.7.4 cannot drive bcrypt 5.x,
# because its backend probe hashes an over-long password and raises ValueError.
bcrypt>=4.0
psutil==5.9.6

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2

# Monitoring
prometheus-client==0.19.0
structlog==23.2.0
